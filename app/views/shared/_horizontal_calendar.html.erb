<div class="d-flex gap-2 my-3 overflow-auto" data-dashboard-target="calendarContainer">
  <% @date_range.each do |date| %>
    <div class="date-card border rounded-4 px-3 text-center cursor-pointer"
          data-action="click->dashboard#loadDateContent"
          data-date="<%= date.to_s %>"
          data-dashboard-target="dateCard">
      <% if date == @current_date %>
        <small>TODAY</small>
      <% end %>
      <div><%= date.day %></div>
      <div><%= date.strftime("%a") %></div>
    </div>
  <% end %>
</div>
